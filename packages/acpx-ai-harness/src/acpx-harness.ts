/**
 * acpx-ai-harness: HarnessV1 adapter for ACP-protocol agents.
 *
 * Implementation lands in subsequent commits. This file exists so the
 * package skeleton typechecks and `bun install` resolves the workspace.
 */

export {}
